[mypy]
python_version = 3.12
ignore_missing_imports = true
explicit_package_bases = true
namespace_packages = true
# package base so npm/ and maven/ shared module basenames don't trip mypy's
# duplicate-module error.
mypy_path = agents/SecurityAdvisories/remediation-workers
exclude = cdk\.out|venv|tests|lambda
