[
  {
    "name": "get_job_info",
    "description": "Get detailed information about a Jenkins job including parameters and requirements. Use this to understand what parameters a job needs before executing it. Does not execute the job.",
    "parameters": {
      "job_name": {
        "description": "Name of the Jenkins job to get information about (defaults to docker-scan)",
        "type": "string",
        "required": false
      }
    },
    "requireConfirmation": "DISABLED"
  },
  {
    "name": "list_jobs",
    "description": "List all Jenkins jobs supported by this agent with their parameters and descriptions. Use when users ask 'what jobs are available?' or need to see all supported Jenkins operations.",
    "parameters": {},
    "requireConfirmation": "DISABLED"
  },
  {
    "name": "trigger_job",
    "description": "Execute any supported Jenkins job with specified parameters. Use for job execution when users specify/confirm a job name and parameters. Supports all registered Jenkins jobs with parameter validation.",
    "parameters": {
      "job_parameters": {
        "description": "JSON object containing job-specific parameters. Each job has different required and optional parameters.",
        "required": "False",
        "type": "string"
      },
      "job_name": {
        "description": "Name of the Jenkins job to trigger (e.g., 'docker-scan', 'Pipeline central-release-promotion, etc.')",
        "required": "False",
        "type": "string"
      }
    },
    "requireConfirmation": "DISABLED"
  }
]

{
  "name": "test_connection",
  "description": "Test connection to Jenkins server. ONLY use when explicitly asked to test connectivity by the user or to troubleshoot connection issues. Do not use this function as part of normal job execution workflow unless explicitly invoked.\n",
  "parameters": {},
  "requireConfirmation": "DISABLED"
}

### `test_connection`
- Tests connection to Jenkins server
- No parameters required
- Use for troubleshooting connectivity issues, ONLY WHEN EXPLICITLY DEMANDED BY THE USER, NOT THROUGH THE REGULAR JENKINS CALLING WORKFLOW
